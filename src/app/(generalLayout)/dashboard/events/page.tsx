import EventContainer from "./_components/EventContainer";

export const metadata = {
  title: "Events",
};

const EventPage = () => {
  return (
    <main>
      <EventContainer />
    </main>
  );
};

export default EventPage;
